/*
    This is just a sample file to demonstrate the feature of preloaded queries
*/

let queries = {
    queries: [
        {
            queryName:'testquery_1',
            query:'ls'

        }
    ]
}